package com.zkkj.gps.gateway.tcp.monitor.test.tcp.demo03;

import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;

public class TestMain {
    public static void main(String[] args) {
        /*DateTimeFormatter df = DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss");
        Date dateTemp = new Date();
        SimpleDateFormat dateFormat = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
        String dateTime = dateFormat.format(dateTemp);
        System.out.println(dateTime);
        LocalDateTime date = LocalDateTime.parse(dateTime,df);
        System.out.println(date.toString());

        LocalDateTime time = LocalDateTime.now();
        DateTimeFormatter dateTimeFormatter = DateTimeFormatter.ofPattern("yyyyMMddHHmmss");
        String createTime = dateTimeFormatter.format(time);
        System.out.println(createTime);*/
        /*DateTimeFormatter df = DateTimeFormatter.ofPattern("yyMMddHHmmss");
        LocalDateTime time = LocalDateTime.now();
        String localTime = df.format(time);
        LocalDateTime date = LocalDateTime.parse(localTime,df);
        System.out.println(date);*/
    }
}
